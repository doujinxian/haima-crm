<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.haima.crm.dao.CustomerCarDao">

	<select id="queryObject" resultType="com.haima.crm.entity.CustomerCar">
		select * from t_customer_car where customer_id = #{value}
	</select>

	<select id="queryList" resultType="com.haima.crm.entity.CustomerCar">
		select * from t_customer_car 
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_customer_car 
	</select>
	 
	<insert id="save" parameterType="com.haima.crm.entity.CustomerCar">
		insert into t_customer_car
		(
			`customer_id`, 
			`vin`, 
			`plate_no`, 
			`del_flg`, 
			`creator`, 
			`create_time`, 
			`modifier`, 
			`modify_time`
		)
		values
		(
			#{customerId}, 
			#{vin}, 
			#{plateNo}, 
			#{delFlg}, 
			#{creator}, 
			#{createTime}, 
			#{modifier}, 
			#{modifyTime}
		)
	</insert>
	 
	<update id="update" parameterType="com.haima.crm.entity.CustomerCar">
		update t_customer_car 
		<set>
			<if test="vin != null">`vin` = #{vin}, </if>
			<if test="plateNo != null">`plate_no` = #{plateNo}, </if>
			<if test="delFlg != null">`del_flg` = #{delFlg}, </if>
			<if test="creator != null">`creator` = #{creator}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="modifier != null">`modifier` = #{modifier}, </if>
			<if test="modifyTime != null">`modify_time` = #{modifyTime}</if>
		</set>
		where customer_id = #{customerId}
	</update>
	
	<delete id="delete">
		delete from t_customer_car where customer_id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_customer_car where customer_id in 
		<foreach item="customerId" collection="array" open="(" separator="," close=")">
			#{customerId}
		</foreach>
	</delete>

</mapper>